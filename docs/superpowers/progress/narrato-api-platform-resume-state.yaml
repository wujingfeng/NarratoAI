protocol: SEGMENTED_GOAL_PROTOCOL_V2
branch: codex/narrato-api-platform
worktree: /private/tmp/NarratoAI-narrato-api-platform
current_gate: "Phase 5 / Gate B pending"
last_complete_task:
  id: Task 15O
  commits:
    - "3b4ea19 feat(api): add terminal project deletion requests (Task 15N)"
    - "8ad2e01 feat(api): persist Core Jianying metadata for Task 15O"
current_task:
  id: Task 15O
  status: completed
  scope: "Persist explicit Core Jianying resource metadata for RegisteredArtifact only."
implementation_commit: "8ad2e01 feat(api): persist Core Jianying metadata for Task 15O"
tests:
  task_15o_red:
    command: "cd docs/api/narratoApi && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest tests/unit/test_artifact_core_manifest_metadata.py -q"
    result: "Expected RED: 2 failed because register_artifact lacked metadata parameters and RegisteredArtifact lacked fields."
  task_15o_green:
    command: "cd docs/api/narratoApi && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest tests/unit/test_artifact_core_manifest_metadata.py tests/unit/test_artifact_model.py tests/unit/test_artifact_registration.py tests/unit/test_jianying_manifest.py -q"
    result: "8 passed in 0.36s."
  task_15o_quality:
    command: "cd docs/api/narratoApi && .venv/bin/ruff check narrato_api/artifacts/models.py narrato_api/artifacts/service.py narrato_api/exports/service.py migrations/versions/0012_artifact_core_manifest_metadata.py tests/unit/test_artifact_core_manifest_metadata.py && .venv/bin/ruff format --check <same files> && git diff --check"
    result: "Passed; five files already formatted."
  task_15o_migration_note:
    command: "cd docs/api/narratoApi && .venv/bin/alembic upgrade head --sql"
    result: "Not a Task 15O pass: blocked at existing 0007_workflow_reconciliation SQLite batch-reflection limitation before 0012."
key_decisions:
  - "RegisteredArtifact now retains optional size, checksum, content_type, width, height, and duration; nullable fields keep prior registered artifacts compatible."
  - "The pure Jianying manifest mapper includes only available metadata and remains free of HTTP, filesystem, OSS, and ZIP operations."
  - "Task 15O does not alter ownership/result eligibility, routes, Core calls, or artifact registration transaction ownership."
remaining_risks:
  - "Real PostgreSQL migration behavior and multi-connection concurrency remain unverified."
  - "The historical offline SQLite Alembic SQL path stops at migration 0007; a fresh online migration must be verified at a later Gate."
  - "Core request/response contract, immutable editor snapshot/timeline input, real Core integration, and browser streaming ZIP remain unimplemented."
  - "Project-wide mypy still has six existing assets-module errors outside Task 15O."
  - "Focused pytest commands require PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 in this environment."
workspace_status:
  clean: false
  expected_uncommitted:
    - path: /.superpowers/
      reason: pre-existing untracked workspace content; do not touch
    - path: docs/web/docs/Oss.php
      reason: pre-existing untracked workspace content; do not touch
next_action:
  id: Task 15P
  scope: "Implement only the typed Core Jianying manifest client boundary that forwards an owned completed project's persisted resources and immutable editor snapshot/timeline inputs; do not create ZIPs, write filesystem/OSS data, change browser UI, or implement real deletion work."
  relevant_files:
    - docs/superpowers/plans/2026-07-16-narrato-api-platform.md
    - docs/superpowers/specs/2026-07-16-narrato-api-platform-design.md
    - docs/api/narratoApi/narrato_api/exports/service.py
    - docs/api/narratoApi/narrato_api/exports/router.py
    - docs/api/narratoApi/narrato_api/core/
    - docs/api/narratoApi/tests/unit/
  acceptance_criteria:
    - "Use TDD: observe a focused RED before minimal implementation."
    - "Only completed owned projects may reach the client boundary; request data derives from persisted artifact metadata and immutable revision input."
    - "No server ZIP, filesystem/OSS write, browser UI change, or later Task 15 work."
    - "Run new and directly affected tests, inspect diff, commit implementation, update and commit checkpoint, then stop."
