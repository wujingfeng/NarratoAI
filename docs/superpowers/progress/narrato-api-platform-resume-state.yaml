branch: codex/narrato-api-platform
worktree: /private/tmp/NarratoAI-narrato-api-platform
current_gate: "Phase 5 / Gate B pending"
last_complete_task:
  id: Task 14B
  commits:
    - "6ea93bf feat: add workflow state machine template (Task 14A)"
    - "60de8c8 feat: add workflow persistence models (Task 14B)"
current_task:
  id: Task 14
  status: in_progress
  completed_subtasks:
    - Task 14A
    - Task 14B
implementation_commit: "60de8c8 feat: add workflow persistence models"
tests:
  task_14b:
    command: "cd docs/api/narratoApi && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest tests/unit/test_workflow_models.py tests/unit/test_workflows_migration.py tests/unit/test_workflow_state_machine.py tests/unit/test_short_drama_template_v1.py -q"
    result: "10 passed, 2 warnings"
  lint:
    command: "cd docs/api/narratoApi && .venv/bin/python -m ruff check narrato_api/workflows/models.py migrations/versions/0006_workflows.py tests/unit/test_workflow_models.py tests/unit/test_workflows_migration.py migrations/env.py"
    result: pass
  commit_check:
    command: "git show --check 60de8c8"
    result: pass
key_decisions:
  - "Task 14A contains only the versioned short-drama DAG template and pure state-machine rules."
  - "Task 14B stores template snapshots, workflow instances, nodes, node attempts, and Outbox records only; it does not implement workflow runtime behavior."
  - "The workflows migration is 0006_workflows, not 0005_workflows, because 0005_asset_probe_reservations already exists; its down_revision is 0005_asset_probe_reservations."
  - "User cancellation and manual retry remain rejected; waiting_for_edit does not auto-timeout or auto-fail."
remaining_risks:
  - "Task 14C still needs pure database-transaction DAG instantiation, persisted state transitions, and Outbox writes."
  - "Core callback/poll reconciliation, Celery dispatch, router, SSE, and Task 15 remain unimplemented."
  - "Real OSS/Core integration and PostgreSQL multi-connection behavior remain unverified."
  - "Focused pytest commands require PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 because default plugin loading is not reliable in this environment."
workspace_status:
  clean: false
  expected_uncommitted:
    - path: /.superpowers/
      reason: pre-existing untracked workspace content
    - path: docs/web/docs/Oss.php
      reason: pre-existing untracked file
next_atomic_task:
  id: Task 14C
  scope: "Only implement the workflow service layer for DAG instantiation, durable state transitions, and Outbox writes as pure database transactions, with focused tests."
  relevant_files:
    - docs/api/narratoApi/narrato_api/workflows/service.py
    - docs/api/narratoApi/narrato_api/workflows/models.py
    - docs/api/narratoApi/tests/unit/test_workflow_service.py
  acceptance_criteria:
    - "A versioned template snapshot can instantiate persisted workflow and node records in one database transaction."
    - "Allowed state transitions update durable workflow/node state and append idempotent Outbox records in the same transaction."
    - "Focused service/model tests pass under PYTEST_DISABLE_PLUGIN_AUTOLOAD=1."
    - "No callback, polling, Celery, dispatcher, router, SSE, Task 15, or other runtime behavior is implemented."
